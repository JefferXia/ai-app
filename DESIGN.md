# 心镜 设计系统

## 产品背景

- **产品定位**：无目的地自我观察的书写空间——打开，写，关掉。帮助用户照见自己
- **目标用户**：有自我觉察需求、愿意安静书写的人群
- **行业领域**：身心健康 / 自我观察 / 轻工具
- **项目类型**：单页书写应用，纸面质感，极简沉浸

## 设计风格

- **风格方向**：纸面书写（Paper & Ink）— 宣纸底、墨色字、极少量装饰
- **装饰程度**：克制 — 无渐变堆砌、无重阴影；质感来自纸色、字距与留白
- **整体基调**：像一张安静的纸，而不是一个应用。无分析、无总结、无追踪
- **布局理念**：书写优先 — 纸张（textarea）占据绝对主体，功能按钮退为幽灵样式

## 字体系统

- **正文/展示**：`"Noto Serif SC", "Songti SC", serif`（shared.tsx 的 `SERIF` 常量，心镜内所有页面统一使用）
- **字距**：中文界面大量使用宽字距营造呼吸感 — 标签 `tracking-[0.2em]`~`tracking-[0.3em]`，按钮 `tracking-[0.25em]`~`tracking-[0.3em]`，标题 `tracking-[0.1em]`~`tracking-[0.12em]`
- **字号**：微标签 10-12px，正文书写 16-18px（`leading-loose`），页面标题 20-24px，落地页主标题 36-48px
- **全局 sans**：Instrument Serif（`--font-instrument-serif`，layout.tsx）仅用于心镜之外的遗留页面

## 颜色系统

深浅双主题，心镜页面通过 `getTheme(dark)`（shared.tsx）取色，`THEME_KEY` localStorage 持久化。

- **亮色（纸）**：
  - 页面底 `#f6f1e7`（米纸） / 卡片浮层 `#fbf7ec`、白 70-90% 透明度
  - 正文 `#33302a`（墨色）
  - 弱化文字 `#b8ad98`、`#c4b9a4`（枯茶）
  - 描边 `#ddd3bf`、`#e4dac6`、`#e0d6c0`
  - 主按钮底 `#4a4232`，悬停 `#5d5340`，文字 `#f6f1e7`
  - 强调文字 `#6b5f47` / `#8a7f6a`（深茶）
  - 错误/危险 `#b0654f`（赭红）
- **暗色（夜）**：
  - 页面底 `#0b0b0c` / 浮层 `#17171a` / 菜单 `#111112`
  - 正文 `gray-300`，弱化 `gray-500`~`gray-700`
  - 描边 `gray-800`
  - 主按钮反转：`bg-gray-200 text-gray-900`，悬停 `bg-white`
- **色彩策略**：几乎无彩色。琥珀色（amber）仅用于会员徽标等有价信息
- **主题切换**：页面左下角日/月图标；暗色切换带 `transition-colors duration-500`

## 间距系统

- **基础单位**：4px（Tailwind 默认）
- **密度**：疏朗 — 大量留白，正文列最大宽度 `max-w-2xl`，落地页特性区 `max-w-3xl`
- **历史流条目间距** `pb-10`~`pb-12`，弹层内边距 `p-8`~`p-10`

## 布局系统

- **书写页**：单列纵向 — 顶部细栏（logo + 菜单开关），历史流（max-h-[40vh]，顶部渐隐 mask），纸张填满剩余空间，操作行钉在纸底
- **子页**：单列居中窄列（`max-w-sm`~`max-w-2xl`），大垂直留白（`py-16`~`py-28`）
- **圆角**：胶囊按钮 `rounded-full`；卡片/浮层 `rounded-xl`~`rounded-2xl`；展开纸 `rounded-sm`
- **层级**：浮层 z-40，抽屉/菜单 z-50；vaul Drawer 用于所有上拉层

## 组件规范

- **主按钮（深色胶囊）**：`rounded-full px-8 py-3 text-xs tracking-[0.3em]`，亮 `bg-[#4a4232] text-[#f6f1e7]` / 暗反转；hover 可 `scale-105`
- **次按钮（描边胶囊）**：同形描边，亮 `border-[#ddd3bf] text-[#6b5f47]`
- **幽灵功能按钮**（引路/照见/翻书）：无边框，`text-[13px] tracking-[0.25em]`，lucide 图标 13px + `fill-transparent`，hover 填充
- **菜单/下拉**：纸面底 + 细边框（亮 `bg-[#fbf7ec] border-[#e5dcc8]` / 暗 `bg-[#17171a] border-gray-800`），菜单项 `text-xs tracking-[0.2em]`
- **提示气泡**（新手引导 TourBubble）：锚定按钮上方，`rounded-xl` 纸面浮层 + 小三角指针 + 深色胶囊「下一步」
- **卡片**（会员页）：半透明 + `backdrop-blur-sm`，hover 轻抬 `-translate-y-1` 并落浅影

## 动效系统

- **入场**：`wx-fade-in`（0.6s ease-out，透明度 + 下移 8px），可叠加 `animation-delay` 错峰
- **打字机**：欢迎信逐字显现，标点处停顿
- **纸团**：归档时纸飞入纸团的轨迹动画；hover 轻抬旋转（见 `archiveStyles`）
- **过渡**：`duration-300` 为主，主题切换 `duration-500`
- **交互确认**：破坏性操作两步确认——第一次进入确认态（文案变红），3 秒内第二次点击生效（如「揉碎这团纸」）

## 交互模式

- **书写即存**：本地 IndexedDB 自动保存（防抖），手动同步云端
- **无打断原则**：引导/提示均为无遮罩小浮层，不阻断书写
- **删除即揉碎**：删除称「揉碎」，确认后内容从本地与云端彻底抹除
- **顶栏**：书写页自带流式顶栏（`WenxinLogo` 墨块胶囊 + 右侧滑出菜单）；子页仅悬浮 logo；落地页自带 header

## 决策日志

| 日期 | 决策 | 理由 |
|------|------|------|
| 2025-03-25 | 初始设计系统创建（Aura 时期） | /design-consultation |
| 2026-09-05 | 全文重写为心镜设计系统 | 产品已从 Aura（AI 陪伴）转向心镜（书写自我观察），旧紫色暗色系废弃，以代码中的实际实现为准 |
